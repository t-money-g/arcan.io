'use strict';
/*
describe('Service: postService', function () {

  // load the service's module
  beforeEach(module('cloneApp'));

  // instantiate service
  var postService;
  beforeEach(inject(function (_postServices_) {
    postService = _postServices_;
  }));

  it('should do something', function () {
    expect(!!postService).toBe(true);
  });

});
*/
